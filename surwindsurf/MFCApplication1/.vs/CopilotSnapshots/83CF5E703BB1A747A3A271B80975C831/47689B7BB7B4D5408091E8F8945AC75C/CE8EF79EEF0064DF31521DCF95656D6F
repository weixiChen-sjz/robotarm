# 医疗穿刺机器人控制系统 - 最终界面布局说明 v3.3

**版本**: v3.3 Simplified  
**日期**: 2025年  
**窗口尺寸**: 1280 × 720 像素（适合1366×768显示器）

---

## 一、整体布局

```
┌──────────────────────────────────────────────────────────────────────────┐
│  医疗穿刺机器人自动化控制系统 v3.2                          [_][□][×]   │
├──────────────────────────────────────────────────────────────────────────┤
│  ┌─232串口┐  ┌─485串口┐  ┌I/O控制┐  ┌─机械臂控制─┐  ┌─流程控制─┐   │
│  │        │  │        │  │       │  │            │  │          │   │
│  │ COM设置│  │ COM设置│  │初始化 │  │ IP+端口    │  │[开始流程]│   │
│  │        │  │        │  │       │  │ [连接][断开│  │[停止][急停│   │
│  │ 发送/  │  │ 发送/  │  │输液   │  │            │  │          │   │
│  │ 接收   │  │ 接收   │  │消毒液 │  │ 当前位置   │  │步骤监控  │   │
│  │        │  │ 距离   │  │激光   │  │ X/Y/Z只读  │  │          │   │
│  │        │  │        │  │       │  │ [刷新]     │  │参数设置  │   │
│  │        │  │        │  │传感器 │  │            │  │          │   │
│  │        │  │        │  │状态   │  │ 目标位置   │  │单步调试  │   │
│  │        │  │        │  │       │  │ X/Y/Z 编辑 │  │          │   │
│  │        │  │        │  │       │  │ 速度调节   │  │          │   │
│  │        │  │        │  │       │  │[移动到目标]│  │          │   │
│  └────────┘  └────────┘  └───────┘  └────────────┘  └──────────┘   │
├──────────────────────────────────────────────────────────────────────────┤
│  ┌─ 系统日志 ─────────────────────────────────────────────────────┐   │
│  │ 时间     │ 模块    │ 级别 │ 消息                                │   │
│  │ 14:30:25 │流程控制 │ INFO │ 开始执行穿刺流程                    │   │
│  │ ...                                                              │   │
│  │                                      [导出日志] [清空日志]      │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│  ● 机械臂: 未连接 | ● 232: 未连接 | ● 485: 未连接 | ● I/O: 未初始化   │
└──────────────────────────────────────────────────────────────────────────┘
```

---

## 二、各区域详细说明

### 2.1 第1列：232串口控制区 (240×460像素)
**位置**: (7, 7) - (247, 467)  
**功能**: 控制232串口通信（电机控制）  
**保持不变**: ✅ 完全保留原有设计

### 2.2 第2列：485串口控制区 (240×460像素)
**位置**: (255, 7) - (495, 467)  
**功能**: 控制485串口通信（激光测距仪）  
**保持不变**: ✅ 完全保留原有设计

### 2.3 第3列：I/O控制区 (200×460像素)
**位置**: (503, 7) - (703, 467)  
**功能**: 控制医疗设备开关  
**保持不变**: ✅ 完全保留原有设计

---

### 2.4 第4列：机械臂控制区 (260×460像素) ⭐简化版⭐

**位置**: (711, 7) - (971, 467)

**功能**: 简化的机械臂控制，去掉微调和预设位

#### 2.4.1 连接设置区（顶部，70px高）

```
┌────────────────────────────────────────────┐
│ IP地址: [192.168.1.10       ]  [连接][断开]│
│ 端口:   [8080    ]  [初始化][归零]  [急停] │
│ 状态: ○ 未连接                             │
└────────────────────────────────────────────┘
```

**控件列表**:
- IP地址编辑框（100px宽）
- 端口编辑框（60px宽）
- 连接按钮、断开按钮
- 初始化按钮、归零按钮
- 急停按钮（大按钮，40px高）
- 状态显示标签

#### 2.4.2 当前位置区（只读显示，60px高）

```
┌────────────────────────────────────────────┐
│ 当前位置（只读）                           │
│ X: [  123.45  ] mm   Z: [  345.67  ] mm   │
│ Y: [  234.56  ] mm   [刷新位置]           │
└────────────────────────────────────────────┘
```

**控件列表**:
- X/Y/Z 三个只读编辑框（70px宽）
- 单位标签 "mm"
- 刷新位置按钮（90px宽）

**功能**:
- 显示机械臂当前坐标
- 点击"刷新位置"手动更新
- ❌ 删除了"自动刷新"复选框

#### 2.4.3 目标位置区（可编辑+移动，85px高）⭐核心功能⭐

```
┌────────────────────────────────────────────┐
│ 目标位置（输入目标坐标）                   │
│ X: [  150.00  ] mm   Z: [  300.00  ] mm   │
│ Y: [  200.00  ] mm   速度: [═══50%═══]    │
│                                            │
│        [→ 移动到目标位置]                  │
└────────────────────────────────────────────┘
```

**控件列表**:
- X/Y/Z 三个可编辑框（70px宽）
- 单位标签 "mm"
- 速度滑块（50px宽，0-100%）
- 速度百分比显示
- **移动到目标位置按钮**（215px宽，20px高，主按钮）

**使用流程**:
1. 在 X/Y/Z 编辑框中输入目标坐标（单位：mm）
2. 调整速度滑块设置移动速度（0-100%）
3. 点击"移动到目标位置"按钮
4. 机械臂自动移动到目标位置

**示例**:
```
输入：X=150, Y=200, Z=300, 速度=50%
点击"移动到目标位置"
→ 机械臂以50%速度移动到 (150, 200, 300)
```

#### ❌ 已删除的功能

1. **微调控制区** - 已删除
   - ❌ 步进选择（0.1mm / 1mm / 10mm）
   - ❌ 6个方向按钮（X±/Y±/Z±）

2. **预设位置区** - 已删除
   - ❌ 保存位置1/2/3按钮
   - ❌ 移动到1/2/3按钮
   - ❌ 预设位置坐标显示

3. **自动刷新**
   - ❌ 自动刷新复选框

### 2.5 第5列：流程控制区 (294×460像素)

**位置**: (979, 7) - (1273, 467)

**功能**: 7步穿刺流程的自动化控制

**保持不变**: ✅ 完全保留原有设计
- 流程控制按钮（开始/停止/急停）
- 流程状态显示
- 步骤监控列表（7步）
- 流程参数设置（5个参数）
- 单步调试（5个测试按钮）

---

### 2.6 底部：系统日志区 (1266×220像素)

**位置**: (7, 475) - (1273, 695)

**功能**: 显示所有操作日志

**尺寸调整**: 
- 从原来的280px高度调整为220px
- 仍然保留4列（时间、模块、级别、消息）
- 日志列表高度175px

---

### 2.7 底部：状态栏

**位置**: 最底部一行 (y=702, 高度18px)

**显示内容**:
- 左侧: 各硬件连接状态
- 右侧: 系统当前时间

---

## 三、窗口尺寸优化

### 3.1 尺寸对比

| 项目 | 旧版本 | 新版本 | 说明 |
|------|--------|--------|------|
| **窗口宽度** | 1600px | **1280px** | 减少320px |
| **窗口高度** | 600px | **720px** | 增加120px |
| **控制区高度** | 280px | **460px** | 增加180px |
| **日志区高度** | 280px | **220px** | 减少60px |

### 3.2 适配常见显示器

| 显示器分辨率 | 窗口占比 | 是否完全显示 |
|-------------|---------|-------------|
| **1366×768** | 93.7% × 93.8% | ✅ 完全显示 |
| **1920×1080** | 66.7% × 66.7% | ✅ 完全显示 |
| **1280×720** | 100% × 100% | ✅ 完全显示（全屏） |
| 1024×768 | 125% × 93.8% | ⚠️ 需要横向滚动 |

**结论**: 新尺寸适合95%以上的现代显示器

---

## 四、机械臂控制区变更总结

### 4.1 保留的功能 ✅

1. **TCP/IP连接**
   - IP地址输入
   - 端口输入
   - 连接/断开按钮
   - 状态显示

2. **基础控制**
   - 初始化
   - 归零
   - 急停

3. **位置显示**
   - 当前位置（X/Y/Z只读）
   - 刷新位置按钮

4. **目标位置移动** ⭐核心功能⭐
   - 目标X/Y/Z输入框
   - 速度调节滑块
   - **移动到目标位置按钮**

### 4.2 删除的功能 ❌

1. **微调控制**
   - ❌ 步进大小选择（0.1/1/10mm）
   - ❌ X轴微调按钮（←→）
   - ❌ Y轴微调按钮（←→）
   - ❌ Z轴微调按钮（↓↑）

2. **预设位置**
   - ❌ 保存位置1/2/3
   - ❌ 移动到位置1/2/3
   - ❌ 预设位置坐标显示

3. **自动刷新**
   - ❌ 自动刷新复选框

### 4.3 控件ID变更

**保留的ID**:
```cpp
// 连接设置
IDC_EDIT_ROBOT_IP               // IP地址
IDC_EDIT_ROBOT_PORT             // 端口
IDC_BUTTON_CONNECT_ROBOT        // 连接
IDC_BUTTON_DISCONNECT_ROBOT     // 断开
IDC_BUTTON_INIT_ROBOT           // 初始化
IDC_BUTTON_HOME_ROBOT           // 归零
IDC_BUTTON_ESTOP_ROBOT          // 急停
IDC_STATIC_ROBOT_STATUS         // 状态显示

// 位置控制
IDC_EDIT_X_POS                  // 当前X（只读）
IDC_EDIT_Y_POS                  // 当前Y（只读）
IDC_EDIT_Z_POS                  // 当前Z（只读）
IDC_BUTTON_REFRESH_POS          // 刷新位置

// 目标位置
IDC_EDIT_TARGET_X               // 目标X
IDC_EDIT_TARGET_Y               // 目标Y
IDC_EDIT_TARGET_Z               // 目标Z
IDC_SLIDER_SPEED                // 速度滑块
IDC_STATIC_SPEED                // 速度显示
IDC_BUTTON_START_ROBOT          // 移动到目标位置
```

**隐藏的ID**（保留定义，但控件不可见）:
```cpp
// 这些ID仍然定义在Resource.h中，但控件已隐藏（NOT WS_VISIBLE）
IDC_CHECK_AUTO_REFRESH          // 自动刷新
IDC_RADIO_STEP_01/1/10          // 步进选择
IDC_BUTTON_X_PLUS/MINUS         // X轴微调
IDC_BUTTON_Y_PLUS/MINUS         // Y轴微调
IDC_BUTTON_Z_PLUS/MINUS         // Z轴微调
IDC_BUTTON_SAVE_POS1/2/3        // 保存预设位
IDC_BUTTON_POS1/2/3             // 移动到预设位
IDC_STATIC_POS1/2/3_INFO        // 预设位信息
```

---

## 五、使用流程示例

### 5.1 连接机械臂

1. 输入IP地址（如：192.168.1.10）
2. 输入端口号（如：8080）
3. 点击"连接"按钮
4. 状态显示：● 已连接
5. 点击"初始化"和"归零"

### 5.2 查看当前位置

1. 点击"刷新位置"按钮
2. 当前位置框显示：X=123.45, Y=234.56, Z=345.67

### 5.3 移动到目标位置 ⭐核心流程⭐

1. **输入目标坐标**
   ```
   X: 150.00
   Y: 200.00
   Z: 300.00
   ```

2. **调整速度**
   - 拖动速度滑块到合适位置（如50%）
   - 速度显示更新为"50%"

3. **点击"移动到目标位置"按钮**
   - 机械臂开始移动
   - 日志显示：机械臂移动到 (150.0, 200.0, 300.0)

4. **移动完成后刷新位置**
   - 点击"刷新位置"
   - 当前位置显示：X=150.00, Y=200.00, Z=300.00

### 5.4 紧急情况

- **急停**: 点击红色"急停"按钮，立即停止所有运动

---

## 六、界面优势

### ✅ 优点

1. **尺寸适配性强**
   - 1280×720适合绝大多数显示器
   - 无需滚动条即可查看所有控件

2. **操作流程简化**
   - 去掉复杂的微调操作
   - 专注于"输入坐标→移动"的核心功能
   - 降低操作难度

3. **布局更紧凑**
   - 5列布局合理利用空间
   - 每列功能明确
   - 日志区域充足

4. **维护性更好**
   - 控件数量减少
   - 代码逻辑更简单
   - 后续扩展更容易

### 💡 适用场景

**适合**:
- ✅ 已知目标坐标的精确定位
- ✅ 自动化流程中的位置移动
- ✅ 通过其他传感器（如视觉、激光）确定目标位置后的移动

**不适合**（已删除）:
- ❌ 手动微调对位（需要微调按钮）
- ❌ 快速切换常用位置（需要预设位）

---

## 七、后续开发建议

### 优先级1（机械臂功能完善）

1. **实现"移动到目标位置"按钮的消息响应**
   ```cpp
   afx_msg void OnBnClickedButtonStartRobot();
   ```
   - 读取目标X/Y/Z编辑框的值
   - 读取速度滑块的值
   - 调用 m_robotArm.MoveTo(pos, speed)

2. **实现"刷新位置"按钮的消息响应**
   ```cpp
   afx_msg void OnBnClickedButtonRefreshPos();
   ```
   - 调用 m_robotArm.GetCurrentPosition()
   - 更新当前X/Y/Z显示框

3. **实现速度滑块的消息响应**
   ```cpp
   afx_msg void OnHScroll(UINT nSBCode, UINT nPos, CScrollBar* pScrollBar);
   ```
   - 更新速度百分比显示

### 优先级2（流程控制）

4. 实现ProcedureController类
5. 实现7步流程自动执行
6. 实现单步测试功能

### 优先级3（可选扩展）

7. 如果需要微调功能，可以添加单独的"高级调试"对话框
8. 如果需要预设位，可以添加到配置文件中

---

## 八、界面尺寸详细参数

| 区域 | X起点 | Y起点 | 宽度 | 高度 | X终点 | Y终点 |
|------|-------|-------|------|------|-------|-------|
| **232串口** | 7 | 7 | 240 | 460 | 247 | 467 |
| **485串口** | 255 | 7 | 240 | 460 | 495 | 467 |
| **I/O控制** | 503 | 7 | 200 | 460 | 703 | 467 |
| **机械臂** | 711 | 7 | 260 | 460 | 971 | 467 |
| **流程控制** | 979 | 7 | 294 | 460 | 1273 | 467 |
| **日志区** | 7 | 475 | 1266 | 220 | 1273 | 695 |
| **状态栏** | 0 | 702 | 1280 | 18 | 1280 | 720 |

**总间距**: 列与列之间8px间距

---

**文档结束**

**版本历史**:
- v3.2 (2025-01-XX): 完整版（包含微调和预设位）
- v3.3 (2025-01-XX): 简化版（去掉微调和预设位，优化窗口尺寸）
